<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljb.dao.StudentMapper">
    <select id="findByUserName" parameterType="String" resultType="com.ljb.pojo.Student">
  		select stu_id as stuId,stu_sn as stuSn,stu_cid as stuCid,stu_pw as stuPw,stu_p as photo,stu_sex as sex,stu_m as remark from student where stu_sn = #{name}
  	</select>
    <insert id="add" parameterType="com.ljb.pojo.Student">
  		insert into student(stu_sn,stu_cid,stu_pw,stu_p,stu_sex,stu_m) values(#{stuSn},#{stuCid},#{stuPw},#{photo},#{sex},#{remark})
  	</insert>

    <update id="edit" parameterType="com.ljb.pojo.Clazz">
  		update student set stu_sn = #{stuSn},stu_cid = #{stuCid},stu_pw = #{stuPw},stu_p = #{photo},stu_sex=#{sex},stu_m = #{remark} where stu_id = #{stuId}
  	</update>
    <update id="delete" parameterType="String">
  		delete from student where stu_id in (${value})
  	</update>
    <select id="findList" parameterType="Map" resultType="com.ljb.pojo.Student">
        select stu_id as stuId,stu_sn as stuSn,stu_cid as stuCid,stu_pw as stuPw,stu_p as photo,stu_sex as sex,stu_m as remark from student where 1=1
        <if test="username !=null">
            and stu_sn like #{username}
        </if>
        <if test="clazzId >0">
            and stu_cid = #{clazzId}
        </if>
        limit #{offset},#{pageSize}
    </select>
    <select id="findAll" resultType="com.ljb.pojo.Student">
  		select stu_id as stuId,stu_sn as stuSn,stu_cid as stuCid,stu_pw as stuPw,stu_p as photo,stu_sex as sex,stu_m as remark from student
  	</select>
    <select id="getTotal" parameterType="Map" resultType="Integer">
        select count(stu_id) from student where 1=1
        <if test="username !=null">
            and stu_sn like #{username}
        </if>
        <if test="gradeId !=null">
            and stu_cid = #{clazzId}
        </if>
    </select>
</mapper>